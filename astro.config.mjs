import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // O domínio final será adicionado aqui (site: 'https://...') quando
  // configurarmos o deploy na AWS (S3 + CloudFront).
});
